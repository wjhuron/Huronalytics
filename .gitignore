# Secrets
service_account.json

# Local data / caches
*.xlsx
*.pkl
mlb_id_cache.json
outlier_results.json
name_duplicates_report.txt
# data_embedded.js is served from GitHub Releases (rolling "data-latest"
# tag), not committed to the repo — keeps git lean despite 50+ MB / 3× daily.
data/data_embedded.js

# Python
.venv/
venv/
__pycache__/
*.pyc

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Claude
.claude/

# Analysis script artifacts
scripts/cache/
scripts/outputs/
